name: Waka Readme

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    name: Update this repo's README
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
          SHOW_TOTAL: "true"
          TIME_RANGE: "all_time"
          SHOW_TITLE: "false"
      
          SHOW_TIME: "true"
          SHOW_LANGUAGE: "true"
          SHOW_LANGUAGE_PER: "true"
          SHOW_BAR: "true"
          SHOW_EDITORS: "true"
          SHOW_OS: "true"
          SHOW_PROJECTS: "false"
      
          TITLE_PREFIX: "🔥 WakaTime Stats —"
          TITLE_SUFFIX: "⚡"
          BLOCKS: "█▓▒░"
          COMMIT_MESSAGE: "chore: update waka stats ✨"
